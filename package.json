{
  "name": "@arcade2d/controls",
  "version": "0.1.2",
  "description": "Game control input library.",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublish": "yarn build"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "devDependencies": {},
  "dependencies": {
    "typescript": "^4.3.2"
  }
}
